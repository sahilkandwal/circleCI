# RNNoise on docker

A dockerized version of [RNNoise](https://github.com/xiph/rnnoise), noise suppression library based on a recurrent neural network.

## Getting started

1. Place your input audio under `examples/noisy.wav`
2. Run the provided docker-compose file
```shell
docker-compose up
```
3. The denoised file is available at `examples/denoised.wav`


### Notes
This denoiser operates on RAW 16-bit (machine endian) mono
PCM files sampled at 48 kHz. It can be used as:

The output is also a 16-bit raw PCM file.
